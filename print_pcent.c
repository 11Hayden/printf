#include "main.h"
/**
* print_cent - prints character % that comes after % to stdout
* @args: variadic parameter
* Return: number of characters printed
*/
int print_cent(va_list args)
{
	(void)args;
	_putchar('%');
return (1);
}
